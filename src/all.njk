---
layout: archive.njk
title: Daniel Rafaj Blog Archive
allignore: true
description: Past, future and present. List of Daniel Rafaj posts.
---
<h1 class="h1">Archive</h1>
{% for post in collections.all | reverse %}
{% if not post.data.allignore %}
  <article class="h-entry hentry">
    <h2 class="p-name entry-title">
      <a class="u-url" href="{{post.url}}" rel="bookmark">
        {{post.url}}
          <p class="text-h4">{{post.data.title}} <span class="italic align-top text-sm tracking-normal">{{ post.data.posttype | default("notype") }}</span></p>
      </a>
    </h2>
    <p class="p-summary entry-summary">{{post.data.description}}</p>
    <em class="italic"><time class="dt-published published" datetime="{{ post.date.toISOString() }}">{{ post.date.toUTCString().substring(0, 16) }}</time></em>
    <!-- <a href="/category/uncategorized/" rel="category tag" class="p-category">General</a> <!-- Example -->
  </article>
{% endif %}
{% endfor %}
